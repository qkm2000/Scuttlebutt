# Dependencies
node_modules/

# Build artifacts (compiled plugin bundle + sourcemap)
/main.js
/main.js.map

# Test build output
.test-out/

# OS / editor cruft
.DS_Store
*.log

# Local WhisperX reference server — users run their own diarizing endpoint
# and point the plugin at it, so this stays out of the plugin repo.
server/

# Secrets — never commit tokens (HF_TOKEN etc.)
.env
*.env

# AI-coder files/folders
.claude

# Design specs / change docs for local review only
docs/changes/
docs/reviews/
